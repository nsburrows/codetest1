extends default

block header

block content
  div.profile
    div.profile-image(style=`background: url('/images/${person.name}.jpg') top center
    no-repeat; background-size: cover;`)
    div.profile-details
      h1.profile-name
        | Name: #{person.name}
      h2.profile-tagline
        | Age: #{person.age}
      h2.profile-tagline
        | College Grad: #{person.is_college_grad}